---
"moor": patch
---

fix(types): export the `MCP_SESSION_IDLE_TTL_MS` constants from the `@moor/types` package entry (`DEFAULT` / `MIN` / `MAX`), so consumers no longer need to hardcode session idle TTL bounds.
